export class data_model {

  constructor(
     public inside_temperature: number,
     public inside_humidity: number,
     public current_date: string,
     public cycle_start_date: string,
     public cycle_length: number

      ) {  }
}
